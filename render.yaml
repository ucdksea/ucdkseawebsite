services:
  - type: web
    name: ucdkseawebsite
    env: node
    rootDir: backend
    region: oregon
    plan: free
    buildCommand: "npm ci"
    startCommand: "npm start"
    healthCheckPath: "/healthz"
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "20"
