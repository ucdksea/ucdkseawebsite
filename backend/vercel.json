{
  "version": 2,
  "builds": [
    { "src": "backend/api/**/*.ts", "use": "@vercel/node" },
    { "src": "next.config.js", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "^/api/(.*)$", "dest": "backend/api/$1" },
    { "src": ".*", "dest": "/" }
  ]
}
